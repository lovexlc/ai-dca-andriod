<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
  <!-- RealtimeChannelService（实时 WebSocket 长连接）需要以前台服务运行。
       Android 14+ 要求 dataSync 型前台服务下发专用权限。 -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  <!-- 帮助在设备重启后主动重连。 -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <!-- 保活引导：请求加入电池优化白名单。 -->
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
  <!-- call=1 持续响铃需要振动。 -->
  <uses-permission android:name="android.permission.VIBRATE" />
  <!-- 查询 / 跳转到可见的应用设置页、发送隐式 Intent 到其他 OEM 设置中心 (如零权限启动、自启动管理)。 -->
  <queries>
    <intent>
      <action android:name="android.settings.APPLICATION_DETAILS_SETTINGS" />
    </intent>
    <intent>
      <action android:name="android.settings.IGNORE_BATTERY_OPTIMIZATION_SETTINGS" />
    </intent>
  </queries>

  <application
    android:name=".AiDcaApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.AiDca">

    <meta-data
      android:name="com.google.firebase.messaging.default_notification_channel_id"
      android:value="ai_dca_messages" />

    <activity
      android:name=".MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <service
      android:name=".NotifyMessagingService"
      android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>

    <!-- 实时 WebSocket 长连接服务。骨架阶段不会被启动，为后续接入预留。 -->
    <service
      android:name=".RealtimeChannelService"
      android:exported="false"
      android:foregroundServiceType="dataSync" />

    <!-- Bark 风 call=1 持续响铃服务。 -->
    <service
      android:name=".CallRingService"
      android:exported="false"
      android:foregroundServiceType="dataSync" />
  </application>

</manifest>
